fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
python-multipart==0.0.12
# moviebox-api installed without deps to avoid Rust/maturin compilation on Render.
# Its dependencies are pinned below manually (mirrors the Termux install method).
rich
click
beautifulsoup4
throttlebuster
